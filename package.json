{
  "name": "combo-worker",
  "version": "0.2.0",
  "private": true,
  "type": "commonjs",
  "description": "Always-on Kalshi combo RFQ worker for aibetbuilder — posts real quotes when kill-switch is disarmed.",
  "scripts": {
    "start": "node start-live.js",
    "test": "node tape.test.js && node engine.test.js && node fills-reader.test.js && node started.test.js && node reserve.test.js && node polymarket-quote.test.js && node skip-tape.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@supabase/supabase-js": "^2.39.0",
    "undici": "^6.21.0"
  }
}
